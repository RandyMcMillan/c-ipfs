services:
  test:
    build:
      context: ..
      dockerfile: docker/Dockerfile.ubuntu-test
    working_dir: /workspace
    volumes:
      - ..:/workspace
    command: ["sh", "-lc", "make clean && make test"]